# CuePlot Drivers

Custom CuePlot device drivers and community extensions.

This repository is structured to be consumed by CuePlot's community driver
browser.

Files:

- `index.json` — driver catalog
- `devices.json` — exact manufacturer/model to driver mapping
- `audio/` — driver files
